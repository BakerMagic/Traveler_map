version: "3.9"

services:
  db:
    image: postgres:16
    container_name: traveler_postgres
    restart: unless-stopped
    env_file:
      - backend/.env
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: